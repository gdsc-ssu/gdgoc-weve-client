import 'package:flutter/material.dart';

class SeniorInputBirthScreen extends StatelessWidget {
  const SeniorInputBirthScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        SizedBox(
          height: 10,
        )
      ],
    );
  }
}
